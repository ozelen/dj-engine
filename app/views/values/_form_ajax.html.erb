<%= form_for @value, remote: true do |f| %>
    <%= f.text_field :value_string %>
    <%= link_to 'Save', '#', onclick: "$.post('#{value_path(@value)}',  {value:{value_string: $(this).prev().val() }} ); ", :class => 'btn btn-primary btn-small' %>
<% end %>