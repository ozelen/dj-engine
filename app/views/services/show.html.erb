
<% content_for :hotel_manage_menu do %>
  <li><%= link_to t('.edit', :default => t("helpers.links.edit")), edit_hotel_service_path(@hotel, @service) %></li>
  <li><%= link_to t('.destroy', :default => t("helpers.links.destroy")), hotel_service_path(@hotel, @service), :method => 'delete', :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) } %></li>
  <li class="divider"></li>
<% end %>

<%= render 'categories/show', category: @service %>