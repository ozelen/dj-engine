<% discussion_of_resort = "#{@resort.node.header}, #{t 'discussion'}" %>
<% title "#{@resort.name}, #{t('comments.h')}" %>

<h1><%= t discussion_of_resort %></h1>

<%= render 'comments/comments_block', commentable_type: 'Resort', commentable: @resort %>