<%= form_for @assignment, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :assigned_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :assigned_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :assigned_type, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :assigned_type, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :role, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :role, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                assignments_path, :class => 'btn' %>
  </div>
<% end %>
