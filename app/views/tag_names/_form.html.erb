<%= form_for @tag_name, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :tag_category_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :tag_category_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :ident, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :ident, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :measure_category_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :measure_category_id, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                tag_names_path, :class => 'btn' %>
  </div>
<% end %>
